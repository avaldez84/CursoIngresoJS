function mostrar() {
    //var cont = 0;
    //var acumulador:
    //do{()
    var lado;
    var perimetro;
    do {
        lado = parseInt(prompt("ingrese el uno de los lados a calcular: "));
    } while (isNaN(lado)|| lado<1);

    perimetro = 3*lado;

    alert('El perimetro es: '+perimetro);


    //realizar un algoritmo que pida los datos necesario para un triangulo equilatero.
    //validar que sea equilatero y mostrar(calcular) el perimetro.
    //















}
